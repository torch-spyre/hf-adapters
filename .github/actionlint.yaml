self-hosted-runner:
  labels:
    - x86_64
    - spyre_pf_x0
    - spyre_pf_x1
    - spyre_pf_x2
    - spyre_pf_x4
    - image_spyre_inference
    - image_torch_spyre
    # used by push-to-clickhouse's generate-matrix job; was missing
    # from this list, so actionlint failed on main independently of image_ingest.
    - image_spyre_backend
    # cardless ClickHouse-ingest set (spyre-frameworks #1235/#1236)
    - image_ingest
