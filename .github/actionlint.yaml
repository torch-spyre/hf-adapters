self-hosted-runner:
  labels:
    - x86_64
    - spyre_pf_x0
    - spyre_pf_x1
    - spyre_pf_x2
    - spyre_pf_x4
    - spyre_pf_x0__pvc_1tb_x1
    - spyre_pf_x1__pvc_1tb_x1
    - spyre_pf_x2__pvc_1tb_x1
    - image_spyre_inference
    - image_torch_spyre
    # Shadow runner-set selector (spyre-frameworks shadow-overlays). Routes jobs
    # onto the isolated hf-adapters shadow set, which boots the pinned /next
    # hf-adapters image. Used only by the DO-NOT-MERGE shadow-validation branch.
    - image_hf_adapters_shadow
